import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // "Reset form state when a modal opens" and "fetch/restore data on
      // mount" are established, React-docs-sanctioned effect patterns used
      // throughout this codebase (every current violation is one of these
      // two, not a real cascading-render bug). Warn instead of blocking CI
      // until these are deliberately refactored.
      "react-hooks/set-state-in-effect": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
